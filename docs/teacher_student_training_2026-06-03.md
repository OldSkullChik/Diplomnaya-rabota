# Teacher-Student Training Snapshot 2026-06-03

This note records the local teacher-student dataset and RuBERT-tiny2 training
state based on the canonical production export
`teacher_student_full_export_2026-06-03_01-06`.

Use `docs/project_data_archive.md` as the canonical data-location index.

## Source Export

Local source folder:

```text
D:\Diplom\data\exports\teacher_student_full_export_2026-06-03_01-06\
```

Production archive:

```text
/home/oldskull/apps/Diplomnaya-rabota/data/exports/teacher_student_full_export_2026-06-03_01-06.tar.gz
```

Source contents:

- `01_gold_approved_annotations.csv`: 5953 approved annotations in the raw export.
- `02_gold_all_annotations_audit.csv`: 6719 total annotations.
- `03_gold_statistics/`: production statistics generated at `2026-06-03T04:06:32+03:00`.
- `silver_batches/`: 259228 unresolved records in 26 numbered CSV batches.

## Silver Labeling

Script:

```text
experiments/label_silver_batches.py
```

Rules module:

```text
data\exports\offline_jkh_labels_all_2026-06-03_00-33\_label_all_jkh.py
```

Outputs:

```text
data\exports\teacher_student_full_export_2026-06-03_01-06\06_silver_auto_labeled_all.csv
data\exports\teacher_student_full_export_2026-06-03_01-06\silver_labeled_batches\
data\exports\teacher_student_full_export_2026-06-03_01-06\06_silver_auto_labeled_all.summary.json
```

Silver labeling counts:

- rows: 259228;
- generated by rules: 244852;
- audited overrides: 14376;
- duplicate records: 0;
- validation errors: 0.

Main silver relevance/topic distribution:

- `jkh_relevance=no`: 183557;
- `jkh_relevance=yes`: 75671;
- `not_jkh`: 183557;
- `waste_cleaning`: 26637;
- `yard_area`: 23354;
- `house_common_property`: 5254;
- `cold_water_sewerage`: 5043;
- `public_authorities`: 4938;
- `heating_hot_water`: 4695;
- `payments_tariffs`: 4679;
- `other_jkh`: 1071.

## Fixed Dataset

Dataset output:

```text
data\ml_experiments\teacher_student_full_2026-06-03_01-06\
```

Counts:

- clean human gold: 4365;
- gold train: 3055;
- gold validation: 655;
- gold test: 655;
- automatic silver train: 259728;
- gold-only fixed dataset: 4365;
- gold+silver fixed dataset: 264093.

Training policy:

- human gold sample weight: 1.0;
- automatic silver sample weight: 0.4;
- validation and test are human gold only;
- 500 automatically applied rows from production are excluded from human gold and included as silver.

## Runs

Gold-only control:

```text
data\ml_experiments\teacher_student_runs\gold_only_full_2026-06-03_01-06\
```

Configuration:

- base model: `cointegrated/rubert-tiny2`;
- heads: `jkh_relevance`, `jkh_topic`, `authority_aspect`, `sentiment`, `appeal_type`, `responsible_party`, `sarcasm`, `quality`;
- text mode: `post_comment`;
- epochs: 4;
- batch size: 16;
- max length: 256;
- device: CUDA.

Gold-only control result:

- test `jkh_relevance` accuracy: 0.9191;
- test `jkh_relevance` macro-F1: 0.4459;
- test `jkh_relevance` weighted-F1: 0.8973.

Interpretation: this is only a control baseline. Accuracy is inflated by class
imbalance; rare multi-class taxonomy heads remain weak with human gold alone.

Gold+silver teacher-student run:

```text
data\ml_experiments\teacher_student_runs\gold_silver_full_2026-06-03_01-06\
```

Configuration:

- base model: `cointegrated/rubert-tiny2`;
- heads: all 8 current taxonomy heads;
- text mode: `post_comment`;
- epochs: 2;
- batch size: 32;
- max length: 256;
- device: CUDA;
- model saving: enabled.

Final outputs:

```text
data\ml_experiments\teacher_student_runs\gold_silver_full_2026-06-03_01-06\train.out.log
data\ml_experiments\teacher_student_runs\gold_silver_full_2026-06-03_01-06\train.err.log
data\ml_experiments\teacher_student_runs\gold_silver_full_2026-06-03_01-06\metrics.json
```

Training history:

- epoch 1: `train_loss=2.5903`, `val_macro_f1=0.3024`;
- epoch 2: `train_loss=2.9005`, `val_macro_f1=0.3046`.

Gold+silver test result on human-gold test set:

| Field | Accuracy | Macro-F1 | Weighted-F1 |
| --- | ---: | ---: | ---: |
| `jkh_relevance` | 0.7863 | 0.4456 | 0.8256 |
| `jkh_topic` | 0.7313 | 0.3022 | 0.8036 |
| `authority_aspect` | 0.6718 | 0.1809 | 0.7633 |
| `sentiment` | 0.4519 | 0.3646 | 0.4264 |
| `appeal_type` | 0.3267 | 0.2478 | 0.3206 |
| `responsible_party` | 0.7008 | 0.1477 | 0.7863 |
| `sarcasm` | 0.5771 | 0.3179 | 0.5727 |
| `quality` | 0.8595 | 0.3622 | 0.8237 |

Comparison output:

```text
data\ml_experiments\teacher_student_runs\teacher_student_comparison_2026-06-03_01-06\
```

Mean macro-F1 comparison:

- gold-only: 0.2249;
- gold+silver: 0.2961;
- delta: +0.0712.

Interpretation: the first full teacher-student run improves mean macro-F1 on
human-gold test data, especially for `jkh_topic`, `appeal_type`, `quality`, and
`sentiment`. This supports the teacher-student approach, but it is not the final
model: next experiments should tune silver weight, epochs, and possibly compare
a larger RuBERT checkpoint if time allows.

## Optimization Sweep

After the first teacher-student run, the training script was extended with
in-memory tokenization caching, weighted-balanced class weights that account for
gold/silver sample weights, sample-weight overrides, dropout configuration, and
streamed progress logging. The sweep runner is:

```text
experiments\run_teacher_student_sweep.py
```

Quick sweep output:

```text
data\ml_experiments\teacher_student_runs\sweep_2026-06-03\
```

Quick result summary:

| Run | Best val macro-F1 | Test mean macro-F1 | Silver weight | Class weights | LR |
| --- | ---: | ---: | ---: | --- | ---: |
| `quick_w03_weighted` | 0.2951 | 0.3000 | 0.3 | `weighted_balanced` | 0.00002 |
| `quick_w04_weighted` | 0.2937 | 0.2999 | 0.4 | `weighted_balanced` | 0.00002 |
| `quick_w02_weighted` | 0.2925 | 0.2997 | 0.2 | `weighted_balanced` | 0.00002 |
| `quick_w05_weighted` | 0.2921 | 0.3000 | 0.5 | `weighted_balanced` | 0.00002 |
| `quick_w03_no_class_weights` | 0.2740 | 0.2661 | 0.3 | `none` | 0.00002 |

Final sweep output:

```text
data\ml_experiments\teacher_student_runs\sweep_final_2026-06-03\
```

Final result summary:

| Run | Best val macro-F1 | Test mean macro-F1 | Silver weight | Class weights | LR |
| --- | ---: | ---: | ---: | --- | ---: |
| `final_w03_weighted_lr1e5_e4` | 0.3086 | 0.3003 | 0.3 | `weighted_balanced` | 0.00001 |
| `final_w03_weighted_e4` | 0.3070 | 0.2935 | 0.3 | `weighted_balanced` | 0.00002 |
| `final_w04_weighted_e4` | 0.3068 | 0.2940 | 0.4 | `weighted_balanced` | 0.00002 |

Best current checkpoint:

```text
data\ml_experiments\teacher_student_runs\sweep_final_2026-06-03\final_w03_weighted_lr1e5_e4\model.pt
data\ml_experiments\teacher_student_runs\sweep_final_2026-06-03\final_w03_weighted_lr1e5_e4\tokenizer\
data\ml_experiments\teacher_student_runs\sweep_final_2026-06-03\final_w03_weighted_lr1e5_e4\metrics.json
```

The best configuration uses `cointegrated/rubert-tiny2`, all 8 heads,
`post_comment`, 4 epochs, batch size 32, max length 256, CUDA,
`silver_weight=0.3`, `weighted_balanced` class weights and `lr=0.00001`.
The best validation epoch was epoch 3; epoch 4 did not improve validation and
the saved checkpoint is the best-validation state.

Gold-test comparison:

| Run | Test mean macro-F1 | `jkh_relevance` | `jkh_topic` | `authority_aspect` | `sentiment` | `appeal_type` | `responsible_party` | `sarcasm` | `quality` |
| --- | ---: | ---: | ---: | ---: | ---: | ---: | ---: | ---: | ---: |
| `gold_only_full` | 0.2249 | 0.4459 | 0.1145 | 0.1243 | 0.3111 | 0.0890 | 0.1216 | 0.3092 | 0.2832 |
| `gold_silver_initial` | 0.2961 | 0.4456 | 0.3022 | 0.1809 | 0.3646 | 0.2478 | 0.1477 | 0.3179 | 0.3622 |
| `quick_w03_weighted` | 0.3000 | 0.4437 | 0.3153 | 0.1686 | 0.3823 | 0.2681 | 0.1527 | 0.3122 | 0.3573 |
| `final_w03_weighted_lr1e5_e4` | 0.3003 | 0.4475 | 0.3088 | 0.1921 | 0.3710 | 0.2519 | 0.1482 | 0.3214 | 0.3612 |

Interpretation: the optimized teacher-student model is the best current
checkpoint by validation and narrowly by held-out gold-test mean macro-F1.
Compared with gold-only, the mean macro-F1 gain is `+0.0754`; compared with the
first full gold+silver run, the gain is `+0.0042`. The largest durable gain
from silver data remains `jkh_topic`, where macro-F1 moved from `0.1145` to
around `0.31`. `responsible_party` is still the weakest head because rare
responsible-party classes remain sparsely represented even after silver
training.

A detailed Russian explanation of the weakest axes, class distributions,
confusion patterns, and the difference between accuracy, weighted F1 and
macro-F1 is stored in:

```text
docs\model_weak_axes_analysis_2026-06-03.md
```

## Diamond Labeled Data Check

A high-confidence diamond layer was built after the first teacher-student sweep:

```text
experiments\build_diamond_dataset.py
data\ml_experiments\diamond_dataset_2026-06-03\
data\exports\diamond_labeled_data_2026-06-03\
data\exports\diamond_labeled_data_2026-06-03.tar.gz
```

Selection method:

- score every silver row with the best current teacher model;
- keep rows where the rule label and model prediction agree on the core heads;
- require minimum confidence and basic taxonomy consistency;
- force in audited offline overrides with higher weight;
- keep the rest of silver only as low-weight residual data.

Final diamond data:

| Layer | Rows | Weight |
| --- | ---: | ---: |
| Human gold | 4365 | 1.00 |
| Audited diamond | 14679 | 0.75 |
| Model-agreed diamond | 146751 | 0.60-0.70 |
| Residual silver | 98298 | 0.20 |

Diamond summary:

- diamond rows: `161430` of `259728` silver rows (`62.15%`);
- diamond JKH-relevant rows: `56760`;
- diamond non-JKH/control rows: `104670`;
- diamond validation found `0` relevance/topic consistency errors;
- import-ready package contains exactly `161430` rows, including `493`
  fallback audited rows reconstructed from the ML diamond dataset.

Verification runs:

| Run | Input | Test mean macro-F1 |
| --- | --- | ---: |
| Previous best `final_w03_weighted_lr1e5_e4` | gold + silver, optimized | 0.3003 |
| `diamond_full_2026-06-03` | gold + diamond + residual silver | 0.2972 |
| `diamond_clean_2026-06-03` | gold + diamond only | 0.2768 |

Interpretation: diamond is the strongest current labeled-data package, but it
does not replace the previous best model checkpoint. For reporting, describe it
as a high-confidence teacher-labeled dataset with explicit weights and audit
metadata. For deployment/model selection, keep using
`final_w03_weighted_lr1e5_e4` unless a later run beats it on the human-gold
validation/test split.
