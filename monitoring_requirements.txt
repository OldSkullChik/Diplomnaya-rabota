# Optional dependencies for the VK monitoring collector.
# Keep these separate from requirements.txt so the basic Django site can run
# without browser/ML packages.
playwright>=1.49,<2.0
transformers>=4.40,<6.0
numpy>=1.26,<3.0
scikit-learn>=1.4,<2.0
